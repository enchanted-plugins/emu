# Fae state files (per-plugin session data)
plugins/*/state/metrics.jsonl
plugins/*/state/memory.jsonl
plugins/*/state/checkpoint.md
plugins/*/state/checkpoint.md.tmp
plugins/*/state/remember.md

# A8 — Skill-Scoped Attribution
plugins/*/state/skill-metrics.jsonl
plugins/*/state/active-skills.json
plugins/*/state/active-skills.json.tmp
plugins/*/state/.session

# A9 — learnings.json migrated to $XDG_DATA_HOME; any local copy is ephemeral
plugins/*/state/learnings.json
plugins/*/state/learnings.json.tmp

# Lock directories
*.lock/

# Temp files
*.tmp

# Secrets
.env

# OS files
.DS_Store
Thumbs.db

# Tmp session caches
/tmp/fae-*

# Asset toolchain (mermaid-cli + mathjax-full are dev-only; SVGs are committed)
docs/assets/node_modules/
docs/assets/package.json
docs/assets/package-lock.json
