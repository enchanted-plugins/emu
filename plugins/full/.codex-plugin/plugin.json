{
  "name": "full",
  "version": "2.0.0",
  "description": "Meta-plugin that installs every Emu plugin at once via dependency resolution. Installs: emu-context-guard, emu-state-keeper, emu-token-saver.",
  "author": {
    "name": "Enchanter Labs"
  },
  "license": "MIT",
  "keywords": [
    "meta",
    "bundle",
    "install-all"
  ],
  "interface": {
    "displayName": "Full",
    "shortDescription": "Meta-plugin that installs every Emu plugin at once via dependency resolution. I\u2026",
    "longDescription": "Meta-plugin that installs every Emu plugin at once via dependency resolution. Installs: emu-context-guard, emu-state-keeper, emu-token-saver.",
    "developerName": "Enchanter Labs",
    "category": "Productivity",
    "capabilities": [
      "Read",
      "Write"
    ],
    "defaultPrompt": [
      "Use Full on my project"
    ]
  }
}
